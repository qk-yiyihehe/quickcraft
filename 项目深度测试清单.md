# QuickCraft 项目深度测试清单

目标：按核心功能逐项深测，优先覆盖边界值、空状态、热键状态切换、第三方模组缺失/存在、以及旧配置迁移。

## 1. 启动与配置
- 启动游戏，确认主模组、客户端入口、ModMenu 和 `mm:early_risers` 都能正常加载。
- 开启/关闭每个功能开关后重进世界，确认状态保持正确。
- 用旧配置文件升级一次，确认新配置和旧键都能被正确读取。
- 切换中英文语言，确认配置说明、按钮文案、状态提示都正常显示。

## 2. 快捷合成

### 2.1 工作台 `QuickCraftWorkbench`
- 空背包、半成品背包、满背包各测一次。
- 手动摆好配方后，测单次合成、连续合成、输出格被堵住时的停止逻辑。
- 测配方缺材料、材料数量不均、剩余材料回收失败、鼠标停在文本框时不触发。

### 2.2 背包合成 `QuickCraftBackpack`
- 2x2 合成、连续合成、输出格阻塞、关闭界面中断各测一次。
- 测玩家主背包、快捷栏、空槽、满槽的回填顺序。
- 测配方缺一格、缺多格、同类物品堆叠不齐时的行为。

### 2.3 切石机 `QuickCraftStonecutter`
- 测单次切石、连续切石、输出格已满、没选配方时的行为。
- 测输入材料不足、配方切换、结果变化、停止后丢出结果的分支。

### 2.4 铁砧改名 `QuickCraftAnvilRename`
- 测单次改名、连续改名、目标名为空、当前名相同、输入框焦点切换。
- 测改名过程中输出被占用、输入物异常变化、提前退出界面。

## 3. 容器工具

### 3.1 同类转移 `QuickTransfer`
- 测容器到背包、背包到容器、鼠标滑过多格时的去重触发。
- 测创造模式、文本框聚焦、光标有物品、空槽、不可取槽。
- 测跨区域拖动时，玩家区和容器区切换后的重置行为。

### 3.2 丢弃 `QuickThrow`
- 测丢单个、丢整组、丢同类匹配、热键按住滑过多格。
- 测文本框聚焦、创造模式、空槽、不可丢槽。
- 测同一手势里重复经过同一格时不重复触发。

### 3.3 排序 `QuickSort`
- 测玩家背包、容器、创造背包三种场景。
- 测空容器、单列容器、双箱、潜影盒内容、光标有物品时的处理。
- 测文本框聚焦、不可排序容器、排序后是否还能恢复选中状态。

### 3.4 交易 `QuickTrade`
- 测村民交易列表重排、收藏交易置顶、恢复原顺序。
- 测交易被售罄、无收藏项、收藏项与当前交易 NBT 不同、交易页滚动状态。
- 测离开界面后状态是否清空，重新打开是否还会误判。

### 3.5 回存 `QuickStash`
- 测箱子、木桶、末影箱、潜影盒四类目标。
- 测容器为空、玩家背包为空、光标有物品、容器已满、部分可回存。
- 测隐藏槽位、未启用槽位、玩家背包与快捷栏的排序顺序。

### 3.6 清空容器 `QuickClearContainer`
- 测有实体库存的容器、末影箱、无实体库存、空容器。
- 测容器里有可丢物、不可取物、玩家光标有物品、界面刚打开就关闭。
- 测容器槽排序是否稳定，是否会误丢玩家背包物品。

### 3.7 容器锁 `QuickContainerLock`
- 测锁按钮显示、槽位锁图标显示、单格锁定、整箱锁定。
- 测锁定后排序是否被拦截，解锁后是否恢复。
- 测玩家背包界面、容器界面、隐藏槽、无效槽、界面切换后的状态保持。

### 3.8 信标 `QuickBeacon`
- 测英文名、中文名、带 `minecraft:` 前缀、带空格/下划线/连字符的效果名。
- 测默认顺序、手动改顺序、重复效果、超过 6 项、空项、无效项。
- 测已拥有效果、持续时间不足、缺少支付物、需要拆矿物块、支付后自动关闭。

### 3.9 容器复制 `QuickContainerCopy`
- 测记录模板、应用模板、容器类型不匹配、光标非空、创造模式填充。
- 测箱子、木桶、漏斗、投掷器、发射器、熔炉、高炉、烟熏炉、酿造台、潜影盒、炼药锅/合成器相关分支。
- 测禁用槽状态、空槽、脏模板、连续填充、quickshulker 存在/不存在。

## 4. 投影与 Litematica

### 4.1 材料收集 `QuickMaterialCollector`
- 测材料 HUD 可见/不可见、请求为空、请求重复、请求数量为 0/负数。
- 测余量区间 `0-10 / 10-20 / 20-50 / 50-100 / 100-500 / 500+`。
- 测 quickshulker 联动开关、背包容量不足、保护物品、替换物品。

### 4.2 容器材料页 `QuickLitematicaContainerMaterials`
- 测 schematic 读取成功/失败、区域过滤、按组合并、隐藏组恢复。
- 测双箱、实体容器、存储潜影盒、名称截断、数量格式化。
- 测按钮位置、列表滚动、详情页返回后 parent 是否保持一致。

### 4.3 容器验证 `QuickLitematicaContainerVerifier`
- 测预期库存与实际库存相同、缺失、额外、错误物品、错误状态。
- 测双箱合并、锁槽位差异、空库存、幽灵物品、slot overlay 位置。
- 测禁用高亮、屏幕绑定清理、没有 Litematica 时的降级。

### 4.4 投影自动填充 `QuickLitematicaContainerAutofill`
- 测右键当前目标、模板类型匹配、不匹配、无模板、无界面、超时。
- 测 quickshulker 开/关、网络包可发送/不可发送、关闭后是否清理绑定。
- 测替换规则介入后，实际填充模板是否按替换结果执行。

### 4.5 替换规则 `QuickLitematicaContainerReplacements`
- 测 `物品1 -> 物品2`、空白、缺分隔符、无效物品、大小写、中文别名。
- 测单个物品、可堆叠物品、不可堆叠物品、数量缩放是否合理。
- 测替换后模板列表是否保持原顺序与空槽占位。

## 5. 配置与兼容
- 测 `craftLoopsPerTick`、材料余量、`quickShulkerActionIntervalTicks` 的上下限夹取。
- 测 `containerFillFreeSlotsLimit` 超范围输入时是否自动修正。
- 测旧键 `quickTradeStandby / quickStashStandby / quickClearContainerStandby / enableQuickContainerCopy / enableQuickClearContainer / enableQuickStash` 的迁移。
- 测 Litematica、quickshulker 缺失时是否只禁用联动，不影响主功能。

## 6. 我在审查里先标出来的高风险点
- `QuickCraftConfigs.loadFromFile()` 先读新配置、再套旧键，若同一个 JSON 里新旧键同时存在，旧键可能覆盖新值，升级遗留配置时要重点验。
- `QuickLitematicaContainerReplacements` 只接受 `A -> B` 这一路径，配置写法稍微变形就会直接失效，建议专门测中英文空格、大小写、别名。
- `QuickContainerCopy` / `QuickMaterialCollector` / `QuickLitematicaContainerAutofill` 都依赖 quickshulker 包和网络包能力，缺模组、包不可发、权限不足时必须走降级分支。

## 7. 建议执行顺序
1. 先测配置与启动。
2. 再测合成、转移、丢弃、排序。
3. 再测交易、容器锁、信标、容器复制。
4. 最后测 Litematica 与 quickshulker 联动。

